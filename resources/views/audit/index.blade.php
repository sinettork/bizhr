<x-layouts::app title="Audit logs">
    <x-workspace-command-bar title="Audit logs" icon="fa-file-shield">
        <x-slot:filters><form class="reference-filter-form" method="GET" hx-get="{{ request()->url() }}" hx-target="[data-list-container]" hx-swap="outerHTML" hx-push-url="true"><div class="input-group reference-search"><span class="input-group-text"><i class="fa-solid fa-magnifying-glass"></i></span><input class="form-control" name="search" value="{{ request('search') }}" placeholder="Event, target, or request ID" hx-get="{{ request()->url() }}" hx-target="[data-list-container]" hx-swap="outerHTML" hx-push-url="false" hx-trigger="keyup changed delay:500ms"></div><select class="form-select reference-status" name="module" hx-get="{{ request()->url() }}" hx-target="[data-list-container]" hx-swap="outerHTML" hx-push-url="true" hx-trigger="change"><option value="">All modules</option>@foreach($modules as $module)<option value="{{ $module }}" @selected(request('module') === $module)>{{ str($module)->replace('_', ' ')->title() }}</option>@endforeach</select><select class="form-select reference-status" name="action" hx-get="{{ request()->url() }}" hx-target="[data-list-container]" hx-swap="outerHTML" hx-push-url="true" hx-trigger="change"><option value="">All actions</option>@foreach($actions as $action)<option value="{{ $action }}" @selected(request('action') === $action)>{{ str($action)->replace('_', ' ')->title() }}</option>@endforeach</select><select class="form-select reference-status" name="user_id" hx-get="{{ request()->url() }}" hx-target="[data-list-container]" hx-swap="outerHTML" hx-push-url="true" hx-trigger="change"><option value="">All actors</option>@foreach($users as $user)<option value="{{ $user->id }}" @selected(request('user_id') == $user->id)>{{ $user->name }}</option>@endforeach</select><input class="form-control reference-status" type="date" name="from" value="{{ request('from') }}" aria-label="From date"><input class="form-control reference-status" type="date" name="to" value="{{ request('to') }}" aria-label="To date"><button class="btn btn-primary reference-search-button">Filter</button></form></x-slot:filters>
    </x-workspace-command-bar>
    <div class="reference-list"><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead><tr><th class="ps-3">Time / Actor</th><th>Action</th><th>Target</th><th>Request context</th><th>Integrity</th><th class="text-end pe-3">Detail</th></tr></thead><tbody>
        @forelse($logs as $log)<tr><td class="ps-3"><div>{{ $log->created_at?->format('d M Y H:i:s') }}</div><small class="text-body-secondary">{{ $log->user?->name ?: 'System' }}</small></td><td><div class="fw-medium">{{ str($log->action)->replace('_', ' ')->title() }}</div><small class="text-body-secondary">{{ str($log->module)->replace('_', ' ')->title() }}</small></td><td><div>{{ class_basename($log->record_type) }} #{{ $log->record_id ?: '—' }}</div><small class="text-body-secondary">{{ $log->event_uuid }}</small></td><td><div>{{ $log->ip_address ?: '—' }}</div><small class="text-body-secondary">{{ $log->route ?: $log->request_id ?: '—' }}</small></td><td><span class="badge text-bg-{{ strlen((string) $log->checksum) === 64 ? 'success' : 'danger' }}">{{ strlen((string) $log->checksum) === 64 ? 'Checksummed' : 'Invalid' }}</span></td><td class="text-end pe-3"><button class="btn btn-action-link btn-sm" type="button" data-bs-toggle="modal" data-bs-target="#audit{{ $log->id }}"><i class="fa-solid fa-eye"></i><span>View</span></button></td></tr>@empty<tr><td class="text-center text-body-secondary py-5" colspan="6">No audit events match the filters.</td></tr>@endforelse
    </tbody></table></div><x-pagination-footer :paginator="$logs" /></div>
    @foreach($logs as $log)<div class="modal fade" id="audit{{ $log->id }}" tabindex="-1"><div class="modal-dialog modal-xl"><div class="modal-content"><div class="modal-header"><h2 class="modal-title fs-5">Audit event {{ $log->event_uuid }}</h2><button class="btn-close" type="button" data-bs-dismiss="modal"></button></div><div class="modal-body"><dl class="row"><dt class="col-sm-3">Actor</dt><dd class="col-sm-9">{{ $log->user?->name ?: 'System' }}{{ $log->user?->email ? ' · '.$log->user->email : '' }}</dd><dt class="col-sm-3">Target</dt><dd class="col-sm-9">{{ $log->record_type }} #{{ $log->record_id }}</dd><dt class="col-sm-3">Route / request</dt><dd class="col-sm-9">{{ $log->route ?: '—' }} · {{ $log->request_id ?: '—' }}</dd><dt class="col-sm-3">Checksum</dt><dd class="col-sm-9 text-break"><code>{{ $log->checksum }}</code></dd></dl><div class="row g-3"><div class="col-md-6"><h3 class="h6">Before</h3><pre class="border rounded bg-body-tertiary p-3 text-wrap">{{ json_encode($log->old_values, JSON_PRETTY_PRINT | JSON_UNESCAPED_UNICODE | JSON_UNESCAPED_SLASHES) ?: '—' }}</pre></div><div class="col-md-6"><h3 class="h6">After</h3><pre class="border rounded bg-body-tertiary p-3 text-wrap">{{ json_encode($log->new_values, JSON_PRETTY_PRINT | JSON_UNESCAPED_UNICODE | JSON_UNESCAPED_SLASHES) ?: '—' }}</pre></div></div></div><div class="modal-footer"><button class="btn btn-light" type="button" data-bs-dismiss="modal">Close</button></div></div></div></div>@endforeach
</x-layouts::app>