<x-layouts::app title="Attendance report">
    <x-workspace-command-bar title="Attendance report" icon="fa-chart-column" context="Reporting">
        <x-slot:actions><x-list-actions :export-url="route('exports.index')" /></x-slot:actions>
    </x-workspace-command-bar>
    <div class="row g-3 mb-4">@foreach ([['Total records', $statistics['total'], 'primary'], ['Present', $statistics['present'], 'success'], ['Late', $statistics['late'], 'warning'], ['Absent', $statistics['absent'], 'danger'], ['Attendance rate', $statistics['attendance_rate'].'%', 'info']] as [$label, $value, $tone])<div class="col-sm-6 col-lg"><div class="card border-0 shadow-sm h-100"><div class="card-body"><div class="small text-body-secondary">{{ $label }}</div><div class="fs-4 fw-semibold text-{{ $tone }}">{{ $value }}</div></div></div></div>@endforeach</div>
    <div class="card border-0 shadow-sm mb-4"><div class="card-body"><form class="row g-3" method="GET" action="{{ route('attendance.reports.index') }}"><div class="col-md-4"><label class="form-label">Search employee</label><input class="form-control" name="search" value="{{ $search }}" placeholder="Name, code, or phone"></div><div class="col-md-2"><label class="form-label">Branch</label><select class="form-select" name="branch_id"><option value="">All branches</option>@foreach ($branches as $branch)<option value="{{ $branch->id }}" @selected((int) $branchId === $branch->id)>{{ $branch->name }}</option>@endforeach</select></div><div class="col-md-2"><label class="form-label">Status</label><select class="form-select" name="status"><option value="">All statuses</option>@foreach (['present', 'late', 'absent', 'on_leave', 'half_day', 'holiday', 'rest_day', 'remote_work', 'business_trip'] as $option)<option value="{{ $option }}" @selected($status === $option)>{{ ucfirst(str_replace('_', ' ', $option)) }}</option>@endforeach</select></div><div class="col-md-2"><label class="form-label">From</label><input class="form-control" type="date" name="date_from" value="{{ $dateFrom }}"></div><div class="col-md-2"><label class="form-label">To</label><input class="form-control" type="date" name="date_to" value="{{ $dateTo }}"></div><div class="col-12 d-flex justify-content-end gap-2"><a class="btn btn-light" href="{{ route('attendance.reports.index') }}">Reset</a><button class="btn btn-primary" type="submit"><i class="fa-solid fa-filter me-2"></i>Apply filters</button></div></form></div></div>
    <div class="card border-0 shadow-sm"><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead class="table-light"><tr><th class="ps-3">Employee</th><th>Date</th><th>Check in / out</th><th>Status</th><th>Late</th><th>Worked</th><th class="pe-3">Overtime</th></tr></thead><tbody>@forelse ($attendances as $attendance)<tr><td class="ps-3"><div class="fw-medium">{{ $attendance->employee?->full_name_km ?: $attendance->employee?->full_name_en ?: $attendance->employee?->employee_code }}</div><small class="text-body-secondary">{{ $attendance->employee?->employee_code }}@if($attendance->employee?->department) · {{ $attendance->employee->department->name }}@endif</small></td><td>{{ $attendance->work_date->format('d M Y') }}</td><td>{{ $attendance->check_in_at?->format('H:i') ?? '—' }} / {{ $attendance->check_out_at?->format('H:i') ?? '—' }}</td><td><span class="badge text-bg-{{ in_array($attendance->status, ['present', 'remote_work', 'business_trip']) ? 'success' : ($attendance->status === 'late' ? 'warning' : 'secondary') }}">{{ ucfirst(str_replace('_', ' ', $attendance->status)) }}</span></td><td>{{ $attendance->late_minutes ? $attendance->late_minutes.' min' : '—' }}</td><td>{{ intdiv((int) $attendance->worked_minutes, 60) }}h {{ (int) $attendance->worked_minutes % 60 }}m</td><td class="pe-3">{{ $attendance->overtime_minutes ? intdiv((int) $attendance->overtime_minutes, 60).'h '.((int) $attendance->overtime_minutes % 60).'m' : '—' }}</td></tr>@empty<tr><td colspan="7" class="p-0"><x-empty-state class="py-5 px-3" icon="fa-chart-column" title="No attendance results" message="No attendance records match the current filters." /></td></tr>@endforelse</tbody></table></div><x-pagination-footer :paginator="$attendances" /></div>
</x-layouts::app>
