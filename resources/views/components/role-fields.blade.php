@props(['permissions', 'role' => null])
<label class="form-label">Role name</label><input class="form-control mb-3" name="name" value="{{ old('name', $role?->name) }}" required><label class="form-label">Permissions</label><div class="row g-3">@foreach($permissions as $module => $modulePermissions)<div class="col-md-4"><fieldset class="border rounded p-3 h-100"><legend class="float-none w-auto px-1 fs-6">{{ str($module)->replace('-', ' ')->title() }}</legend>@foreach($modulePermissions as $permission)<label class="form-check"><input class="form-check-input" type="checkbox" name="permissions[]" value="{{ $permission->name }}" @checked(in_array($permission->name, old('permissions', $role?->permissions?->pluck('name')->all() ?? []), true))><span class="form-check-label">{{ str($permission->name)->after('.')->replace('-', ' ')->title() }}</span></label>@endforeach</fieldset></div>@endforeach</div>
